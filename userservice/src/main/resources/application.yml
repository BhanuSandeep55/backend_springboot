spring: 
  application.name: userservice
  datasource:
    url: jdbc:h2:mem:testdb;NON_KEYWORDS=User
    driverClassName: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server.port: 8083
#inventory-host-url: http://inventoryservice:8082